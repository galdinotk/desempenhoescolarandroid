<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.desempenhoescolarapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/boletim"
        android:label="@string/app_name"
        android:roundIcon="@drawable/boletim"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".moduloAluno"></activity>
        <activity android:name=".moduloProfessor" />
        <activity android:name=".testes" />
        <activity android:name=".moduloAdministrador" />
        <activity android:name=".erroGeral" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>